language: python:3.6
before-install:
  - cd DataPostProcess
install:
  - pip3 install -r requirements.txt
script:
  - echo "Hi from Post process"
after-success:
  - docker login -u $username -p $password
  - echo $TRAVIS_BUILD_NUMBER
  - docker build -t teamyoda/datapostprocess:$TRAVIS_BUILD_NUMBER .
  - docker push teamyoda/datapostprocess:$TRAVIS_BUILD_NUMBER
branches:
  only:
    - DataPostProcessorAPI
