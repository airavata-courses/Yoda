language: python:3.7
before-install:
  - cd DataPostProcess
install:
  - pip3 install -r requirements.txt
script:
  - echo "Hi from Post process"
after-success:
  - docker login -u $username -p $password
  - docker build -t teamyoda/datapostprocess:1.0 .
  - docker push teamyoda/datapostprocess:1.0
branches:
  only:
    - DataPostProcessorAPI
