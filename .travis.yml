matrix:
include: 
    language: java
    jdk:
      - openjdk8

    python: 
        - "3.6"  
    script: 
      - cd Session/KafkaConsumer
      - mvn clean install
      - mvn spring-boot:run
    l - cd ..
      - cd ..
      - cd DataModel
      - python -m venv env
      - source env/bin/activate
      - pip install -r requirements.txt

    branches:  
      only:
        - 118-a