apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-dev-2
  labels:
    app: frontend-dev
    version: v2
spec:
  minReadySeconds: 5
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 60
  strategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: frontend-dev
      version: v2
  template:
    metadata:
      labels:
        app: frontend-dev
        version: v2
    spec:
      containers:
      - name: frontend-dev
        image: teamyoda/userinterface-v2
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        resources:
          limits:
            cpu: 2000m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 64Mi